# Divergences from pydantic

Perldantic aims to behave exactly like pydantic for the same schema and input. Any difference
not listed here is a bug.

Each entry records the schema type, the pydantic behaviour, the Perldantic behaviour, and why
they differ.

| # | Area | pydantic | Perldantic | Reason |
|---|---|---|---|---|
| 1 | Python-only schema types | `complex`, `fraction`, `named-tuple`, `dataclass`, `deque`, `counter`, `ordered-dict`, `frozendict`, `is-subclass`, `missing-sentinel`, `json-or-python` | Rejected with a schema error | These types have no Perl counterpart (see `upstream/UPSTREAM.md`) |
| 2 | Error URLs | `https://errors.pydantic.dev/<pydantic major.minor>/v/<type>` | `https://errors.pydantic.dev/latest/v/<type>` | The core has no pydantic version; `latest` is upstream's own fallback |
| 3 | URL toggle in `str(ValidationError)` | `PYDANTIC_ERRORS_INCLUDE_URL` | `PERLDANTIC_ERRORS_INCLUDE_URL` (same values) | Separate namespace for a separate library |
| 4 | `value_error` / `assertion_error` context | `ctx.error` holds the Python exception object | `ctx.error` holds the exception message string | There is no Python exception object; the message is what gets rendered |
| 5 | `repr()` of non-ASCII strings in error output | Uses Python's full Unicode `isprintable()` table | Escapes controls, separators, common format characters and private-use code points; other code points print as-is | No Unicode database in the core; differs only for rare unassigned or format characters |
| 6 | Invalid schemas | Validated against a generated self-schema first; errors read `Invalid Schema: ...` with a structured error list | Reported while building validators, as the first `SchemaError` / `KeyError` / `TypeError` found, e.g. `'strict' should be a bool, got str` | There is no Python-generated self-schema; the core schema format itself is unchanged |
| 7 | `regex_engine="python-re"` | Patterns run with Python's `re` module | Schema error `Invalid regex engine: python-re`; only `rust-regex` (the default) is available | There is no Python `re` outside Python; a Perl-regex engine through host callbacks may follow |
| 8 | Errors for Perl input (`InputType::Perl`, the FFI default for host data) | pydantic's codes (`dict_type`, `list_type`, `tuple_type`, `set_type`, `none_required`, `float_type`, `float_parsing`, `int_from_float`, `time_delta_type`, `callable_type`, ...), messages ("a valid dictionary / list / tuple / None / bytes / timedelta"), contexts (`field_type` `List`, class `Decimal`), Python reprs and type names of the input (`None`, `True`, `{'a': 1}`, `input_type=dict`) and documentation links; a list fails a strict tuple | Perl's vocabulary: codes `hash_type`, `array_type` (every sequence), `undef_required`, `number_type`, `number_parsing`, `int_from_fraction`, `duration_type`, `code_type` (also accepted by `ErrorType::new`); messages "a hash reference / an array reference / undef / a valid byte string / a valid duration"; contexts `Array` / `Hash` and Perl classes (`Math::BigFloat`, `Perldantic::Uuid`, ...); input values as Perl data (`undef`, `!!1`, `{a => 1}`, `bless({...}, 'Class')`) with Types::Standard type names (`Str`, `Num`, `HashRef`, ...); no links; serializer warnings for Perl data (`SerializeOptions.input_type`) read "Perldantic serializer warnings: Expected `ArrayRef[Int]` - ..." with the same Perl names and values instead of `PydanticSerializationUnexpectedValue(Expected `list[int]` ...)`; an array is a strict tuple; `input_type => 'python'` restores pydantic's behaviour | Perl has hashes, arrays, undef, numbers and durations, not Python's types; pydantic's documentation pages are named by pydantic's codes |
| 9 | Order of Perl hash keys (the wire encoder) | Dicts keep insertion order | Perl hashes are converted with sorted keys | Perl hashes have no order; sorting makes outputs and errors deterministic |
| 10 | Invalid `default` with `validate_default` and `on_error='default'` | Recurses until the process crashes (stack overflow) | The default's own validation error is raised | A crash is never the intended behaviour |
| 11 | Schema values upstream never expects (`on_error` outside `raise`/`omit`/`default`, `variadic_item_index` out of range, `custom-error` without `custom_error_type`) | Rust panic (`PanicException`) | Schema error naming the bad value | Upstream relies on its self-schema to reject them first; a panic must never reach the host |
| 12 | Order of set items turned into a list or tuple | Python's hash order (`[1, '2', b'3']` from `{b'3', 1, '2'}`) | The order the set was given in | Hash order is an accident of CPython; Perl has no sets, so hosts pass them in a defined order |
| 13 | Classes (`model`, `enum`, `typed-dict` and `is-instance` schemas) | `cls` is a Python class; instances of subclasses count as instances, and attribute access follows Python (proxies, `__getattr__`); enum members are the class's own objects | `cls` is a class name (a Perl package); only instances with that exact name count, attributes are the instance's fields; enum members carry their class name, member name and value; `is-instance` takes builtin values by their Python type name and host objects by the ancestry the host gives; error messages name the class by `cls_repr` (the class name when absent), and JSON Schema errors by `cls` | The core has no class objects; Perl class hierarchies will come through the host bridge |
| 14 | Model instances serialized by inference (`any` schemas, unexpected values) | Serialized with the model class's own serializer | Serialized as a dict of the instance's fields and extra values | The core has no registry of model classes and their serializers |
| 15 | Model config in JSON Schema generation | Read from the model class's `model_config` (`title`, `extra`, `json_schema_extra`, `json_schema_mode_override`, …); the class docstring becomes the `description`; callables (`json_schema_extra` functions, `model_title_generator`, `pydantic_js_functions`) customise the schema | Read from the `model` schema's `config` (and, for the whole schema, the `config` argument that stands in for a `TypeAdapter`'s), with core config names (`title`, `extra_fields_behavior`, `str_min_length`, `ser_json_bytes`, `validate_by_alias`) plus pydantic's `json_schema_extra` (dict only), `json_schema_mode_override` and `json_schema_serialization_defaults_required`; descriptions come from `metadata.pydantic_js_updates`; callables are a schema error | The core has no class objects; the host passes what pydantic reads off the class as schema data |
| 16 | `now_op` (past/future dates and datetimes) without `now_utc_offset` | Compared with the local time of the Python process (`time.localtime().tm_gmtoff`) | Compared with UTC | The core has no access to the host's local timezone; hosts that need local time pass `now_utc_offset` |
| 17 | `decimal` with `multiple_of` when `(value / multiple_of) % 1` is undefined in Python's default context (quotient too large for 28 digits, division by zero, infinities) | `decimal.InvalidOperation` / `DivisionByZero` escapes validation | A `ValueError` naming the operation escapes validation | The core implements Python's decimal arithmetic itself; there are no `decimal` exception classes |
| 18 | Decimals in Perl (`Math::BigFloat`) | `Decimal` keeps negative zero, NaN payloads and signaling NaNs | `-0` becomes `0` and every NaN a quiet NaN once converted to `Math::BigFloat`; a decoded object remembers the core's text (so `Decimal('2.0')` round-trips) until its value changes | Math::BigFloat has no negative zero, NaN payloads or signaling NaNs, and drops trailing zeros |
| 19 | Perl arrays where tuples, sets and frozensets are expected (Perl data, the FFI default) | A `set` schema's input must be a `set` in strict mode; serializers warn about a list given for a `tuple`, `set` or `frozenset` | A Perl array is an exact set / frozenset input, and `tuple`, `set` and `frozenset` serializers take it without warning; `input_type => 'python'` restores pydantic's behaviour | Perl has only arrays; they stand for every sequence type |
| 20 | Computed fields (`computed_fields` of `model-fields` and `typed-dict` schemas) | The value is `getattr(model, property_name)` on the Python object | Each `computed-field` schema also carries `function`, a host function the core calls with the model and the property name; a schema without it is a schema error | The core has no host objects to read properties from; the host passes its getter, as it passes validator functions |
| 21 | Field count check of model serializers (union member choice, `SerCheck`) | Every model field counts: an instance always has them all | Fields whose schema is a `default` wrapper do not count as required | Perl model objects leave out attributes that were never set and have a default; they must still pick their own model in a union instead of warning |
