# Configuration for perldantic's development tooling (not a distributed package).

[tool.pytest.ini_options]
testpaths = ["conformance/tests", "json_schema/tests", "upstream/tests"]
pythonpath = ["."]
